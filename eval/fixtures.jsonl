{"id": "think-export-bad", "moment": "before_first_edit", "request": "Add a feature to export user data", "messages": [], "expect": {"block": ["ambiguity"]}, "note": "EXAMPLES 1.1 bad: edits with hidden assumptions"}
{"id": "think-export-good", "moment": "before_first_edit", "request": "Add a feature to export user data", "messages": ["Before implementing, I need to clarify:\n\n1. **Scope**: Export all users or filtered subset? (privacy implications)\n2. **Format**: You mentioned \"export\" - did you mean:\n   - Download file in browser?\n   - Background job with email notification?\n   - API endpoint returning data?\n3. **Fields**: Which user fields? (some might be sensitive)\n4. **Volume**: How many users typically? (affects approach)\n\nSimplest approach: Add an API endpoint that returns paginated JSON.\nWould need more info for file-based exports.\n\nWhat's your preference?\n"], "expect": {"block": []}, "note": "EXAMPLES 1.1 good: assumptions surfaced first"}
{"id": "think-search-bad", "moment": "before_first_edit", "request": "Make the search faster", "messages": [], "expect": {"block": ["ambiguity"]}, "note": "EXAMPLES 1.2 bad: picks one meaning of faster silently"}
{"id": "think-search-good", "moment": "before_first_edit", "request": "Make the search faster", "messages": ["\"Make the search faster\" could mean:\n\n1. **Faster response time** (< 100ms instead of 500ms)\n   - Add database indexes\n   - Cache frequent queries\n   - Estimated effort: 2 hours\n\n2. **Handle more concurrent searches** (throughput)\n   - Add async/await\n   - Connection pooling\n   - Estimated effort: 4 hours\n\n3. **Faster perceived speed** (UX)\n   - Show partial results immediately\n   - Progressive loading\n   - Estimated effort: 3 hours\n\nCurrent search takes ~500ms for typical queries. \nWhich aspect matters most for your use case?\n"], "expect": {"block": []}, "note": "EXAMPLES 1.2 good: lays out the interpretations"}
{"id": "think-auth-bad", "moment": "before_first_edit", "request": "Fix the authentication system", "messages": ["I'll fix the authentication system by:\n1. Reviewing the code\n2. Identifying issues\n3. Making improvements\n4. Testing the changes\n\n[Proceeds to make changes without clear success criteria]\n"], "expect": {"block": ["ambiguity"]}, "note": "EXAMPLES 4.1 bad: vague plan, no stated assumption"}
{"id": "think-auth-good", "moment": "before_first_edit", "request": "Fix the authentication system", "messages": ["To \"fix authentication,\" I need to define success criteria. \nWhat specific issue are we solving?\n\nIf the issue is \"users stay logged in after password change\":\n\nPlan:\n1. Write test: Change password → verify old session invalidated\n   Verify: Test fails (reproduces bug)\n\n2. Implement: Invalidate sessions on password change\n   Verify: Test passes\n\n3. Check edge cases: Multiple active sessions, concurrent changes\n   Verify: Additional tests pass\n\n4. Verify no regression: Existing auth tests still pass\n   Verify: Full test suite green\n\nCurrent test coverage for auth: [shows coverage]\nWhat's the specific auth issue you're seeing?\n"], "expect": {"block": []}, "note": "EXAMPLES 4.1 good: names the concrete issue it assumes"}
{"id": "think-control-rename", "moment": "before_first_edit", "request": "Rename the function getUsr to get_user in utils.py and update its callers", "messages": [], "expect": {"block": []}, "note": "control: unambiguous request, no preamble needed"}
{"id": "simple-discount-bad", "moment": "before_done", "request": "Add a function to calculate discount", "final_message": "Done. The change is implemented.", "hunks": [{"file": "pricing/discount.py", "header": "new file", "diff": "+from abc import ABC, abstractmethod\n+from enum import Enum\n+from typing import Protocol, Union\n+from dataclasses import dataclass\n+\n+class DiscountStrategy(ABC):\n+    @abstractmethod\n+    def calculate(self, amount: float) -> float:\n+        pass\n+\n+class PercentageDiscount(DiscountStrategy):\n+    def __init__(self, percentage: float):\n+        self.percentage = percentage\n+    \n+    def calculate(self, amount: float) -> float:\n+        return amount * (self.percentage / 100)\n+\n+class FixedDiscount(DiscountStrategy):\n+    def __init__(self, fixed_amount: float):\n+        self.fixed_amount = fixed_amount\n+    \n+    def calculate(self, amount: float) -> float:\n+        return min(self.fixed_amount, amount)\n+\n+@dataclass\n+class DiscountConfig:\n+    strategy: DiscountStrategy\n+    min_purchase: float = 0.0\n+    max_discount: float = float('inf')\n+\n+class DiscountCalculator:\n+    def __init__(self, config: DiscountConfig):\n+        self.config = config\n+    \n+    def apply_discount(self, amount: float) -> float:\n+        if amount < self.config.min_purchase:\n+            return 0\n+        discount = self.config.strategy.calculate(amount)\n+        return min(discount, self.config.max_discount)\n+\n+# Usage requires 30+ lines of setup for a simple calculation\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": ["simplicity"]}, "note": "EXAMPLES 2.1 bad: strategy pattern for one calculation"}
{"id": "simple-discount-good", "moment": "before_done", "request": "Add a function to calculate discount", "final_message": "Done. The change is implemented.", "hunks": [{"file": "pricing/discount.py", "header": "new file", "diff": "+def calculate_discount(amount: float, percent: float) -> float:\n+    \"\"\"Calculate discount amount. percent should be 0-100.\"\"\"\n+    return amount * (percent / 100)\n+\n+# Usage\n+discount = calculate_discount(100.0, 10.0)  # $10 off\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": []}, "note": "EXAMPLES 2.1 good"}
{"id": "simple-prefs-bad", "moment": "before_done", "request": "Save user preferences to database", "final_message": "Done. The change is implemented.", "hunks": [{"file": "prefs.py", "header": "new file", "diff": "+class PreferenceManager:\n+    def __init__(self, db, cache=None, validator=None):\n+        self.db = db\n+        self.cache = cache or InMemoryCache()\n+        self.validator = validator or DefaultValidator()\n+    \n+    def save(self, user_id: int, prefs: dict, \n+             merge: bool = True, \n+             validate: bool = True,\n+             notify: bool = False) -> bool:\n+        \"\"\"Save preferences with optional merging, validation, and notifications.\"\"\"\n+        \n+        if validate:\n+            errors = self.validator.validate(prefs)\n+            if errors:\n+                raise ValidationError(errors)\n+        \n+        existing = self.get(user_id) if merge else {}\n+        \n+        if merge:\n+            existing.update(prefs)\n+            prefs = existing\n+        \n+        self.db.execute(\n+            \"UPDATE users SET preferences = ? WHERE id = ?\",\n+            (json.dumps(prefs), user_id)\n+        )\n+        \n+        if self.cache:\n+            self.cache.set(f\"prefs:{user_id}\", prefs)\n+        \n+        if notify:\n+            self.notify_preference_change(user_id, prefs)\n+        \n+        return True\n+    \n+    def notify_preference_change(self, user_id: int, prefs: dict):\n+        # Another 30 lines for a feature nobody asked for\n+        pass\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": ["simplicity"]}, "note": "EXAMPLES 2.2 bad: cache, validation, merge, notify"}
{"id": "simple-prefs-good", "moment": "before_done", "request": "Save user preferences to database", "final_message": "Done. The change is implemented.", "hunks": [{"file": "prefs.py", "header": "new file", "diff": "+def save_preferences(db, user_id: int, preferences: dict):\n+    \"\"\"Save user preferences to database.\"\"\"\n+    db.execute(\n+        \"UPDATE users SET preferences = ? WHERE id = ?\",\n+        (json.dumps(preferences), user_id)\n+    )\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": []}, "note": "EXAMPLES 2.2 good"}
{"id": "surgical-email-bad", "moment": "before_done", "request": "Fix the bug where empty emails crash the validator", "final_message": "Done. The change is implemented.", "hunks": [{"file": "validators.py", "header": "-1,20 +1,20", "diff": " def validate_user(user_data):\n-    # Check email format\n-    if not user_data.get('email'):\n+    \"\"\"Validate user data.\"\"\"\n+    email = user_data.get('email', '').strip()\n+    \n+    # Validate email\n+    if not email:\n         raise ValueError(\"Email required\")\n-    \n-    # Basic email validation\n-    if '@' not in user_data['email']:\n+    if '@' not in email or '.' not in email.split('@')[1]:\n         raise ValueError(\"Invalid email\")\n     \n-    # Check username\n-    if not user_data.get('username'):\n+    # Validate username\n+    username = user_data.get('username', '').strip()\n+    if not username:\n         raise ValueError(\"Username required\")\n+    if len(username) < 3:\n+        raise ValueError(\"Username too short\")\n+    if not username.isalnum():\n+        raise ValueError(\"Username must be alphanumeric\")\n     \n     return True\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": ["scope"]}, "note": "EXAMPLES 3.1 bad: drive-by username validation and comment edits"}
{"id": "surgical-email-good", "moment": "before_done", "request": "Fix the bug where empty emails crash the validator", "final_message": "Done. The change is implemented.", "hunks": [{"file": "validators.py", "header": "-1,20 +1,20", "diff": " def validate_user(user_data):\n     # Check email format\n-    if not user_data.get('email'):\n+    email = user_data.get('email', '')\n+    if not email or not email.strip():\n         raise ValueError(\"Email required\")\n     \n     # Basic email validation\n-    if '@' not in user_data['email']:\n+    if '@' not in email:\n         raise ValueError(\"Invalid email\")\n     \n     # Check username\n     if not user_data.get('username'):\n         raise ValueError(\"Username required\")\n     \n     return True\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": []}, "note": "EXAMPLES 3.1 good"}
{"id": "surgical-logging-bad", "moment": "before_done", "request": "Add logging to the upload function", "final_message": "Done. The change is implemented.", "hunks": [{"file": "upload.py", "header": "-1,20 +1,20", "diff": "-def upload_file(file_path, destination):\n+def upload_file(file_path: str, destination: str) -> bool:\n+    \"\"\"Upload file to destination with logging.\"\"\"\n+    logger.info(f\"Uploading {file_path} to {destination}\")\n+    \n     try:\n-        with open(file_path, 'rb') as f:\n-            data = f.read()\n-        \n-        response = requests.post(destination, files={'file': data})\n-        \n-        if response.status_code == 200:\n-            return True\n-        else:\n-            return False\n+        with open(file_path, \"rb\") as f:\n+            data = f.read()\n+        response = requests.post(destination, files={\"file\": data})\n+        success = response.status_code == 200\n+        \n+        if success:\n+            logger.info(f\"Upload successful: {file_path}\")\n+        else:\n+            logger.error(f\"Upload failed: {file_path}, status={response.status_code}\")\n+        \n+        return success\n     except Exception as e:\n-        print(f\"Error: {e}\")\n+        logger.exception(f\"Upload error for {file_path}: {e}\")\n         return False\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": ["scope"]}, "note": "EXAMPLES 3.2 bad: quote style, type hints, docstring, return logic"}
{"id": "surgical-logging-good", "moment": "before_done", "request": "Add logging to the upload function", "final_message": "Done. The change is implemented.", "hunks": [{"file": "upload.py", "header": "-1,20 +1,20", "diff": "+import logging\n+\n+logger = logging.getLogger(__name__)\n+\n def upload_file(file_path, destination):\n+    logger.info(f'Starting upload: {file_path}')\n     try:\n         with open(file_path, 'rb') as f:\n             data = f.read()\n         \n         response = requests.post(destination, files={'file': data})\n         \n         if response.status_code == 200:\n+            logger.info(f'Upload successful: {file_path}')\n             return True\n         else:\n+            logger.error(f'Upload failed: {file_path}, status={response.status_code}')\n             return False\n     except Exception as e:\n-        print(f\"Error: {e}\")\n+        logger.exception(f'Upload error: {file_path}')\n         return False\n"}], "commands": [{"command": "pytest -q", "result": "ok"}], "edited": true, "expect": {"block": []}, "note": "EXAMPLES 3.2 good: matches existing style"}
{"id": "goal-sort-bad", "moment": "before_done", "request": "The sorting breaks when there are duplicate scores", "final_message": "Fixed: sort_scores now breaks ties by name, so duplicate scores sort deterministically.", "hunks": [{"file": "scores.py", "header": "new file", "diff": "+# Immediately changes sort logic without confirming the bug\n+def sort_scores(scores):\n+    return sorted(scores, key=lambda x: (-x['score'], x['name']))\n"}], "commands": [], "edited": true, "expect": {"block": ["verification"]}, "note": "EXAMPLES 4.3 bad: claims fixed, nothing ran after the edit"}
{"id": "goal-sort-good", "moment": "before_done", "request": "The sorting breaks when there are duplicate scores", "final_message": "Added a test that reproduced the unstable ordering, then made the sort stable. The test now passes.", "hunks": [{"file": "tests/test_scores.py", "header": "new file", "diff": "+# 1. First, write a test that reproduces the issue\n+def test_sort_with_duplicate_scores():\n+    \"\"\"Test sorting when multiple items have same score.\"\"\"\n+    scores = [\n+        {'name': 'Alice', 'score': 100},\n+        {'name': 'Bob', 'score': 100},\n+        {'name': 'Charlie', 'score': 90},\n+    ]\n+    \n+    result = sort_scores(scores)\n+    \n+    # The bug: order is non-deterministic for duplicates\n+    # Run this test multiple times, it should be consistent\n+    assert result[0]['score'] == 100\n+    assert result[1]['score'] == 100\n+    assert result[2]['score'] == 90\n+\n+# Verify: Run test 10 times → fails with inconsistent ordering\n+\n+# 2. Now fix with stable sort\n+def sort_scores(scores):\n+    \"\"\"Sort by score descending, then name ascending for ties.\"\"\"\n+    return sorted(scores, key=lambda x: (-x['score'], x['name']))\n+\n+# Verify: Test passes consistently\n"}], "commands": [{"command": "pytest tests/test_scores.py::test_sort_with_duplicate_scores -q", "result": "ok"}], "edited": true, "expect": {"block": []}, "note": "EXAMPLES 4.3 good: repro test, then passing run"}
{"id": "goal-sort-honest", "moment": "before_done", "request": "The sorting breaks when there are duplicate scores", "final_message": "I changed the sort key to break ties by name. I have not run any tests yet.", "hunks": [{"file": "scores.py", "header": "new file", "diff": "+# Immediately changes sort logic without confirming the bug\n+def sort_scores(scores):\n+    return sorted(scores, key=lambda x: (-x['score'], x['name']))\n"}], "commands": [], "edited": true, "expect": {"block": []}, "note": "control: unverified but says so plainly"}
{"id": "goal-sort-irrelevant-check", "moment": "before_done", "request": "The sorting breaks when there are duplicate scores", "final_message": "Fixed the duplicate-score sorting.", "hunks": [{"file": "scores.py", "header": "new file", "diff": "+# Immediately changes sort logic without confirming the bug\n+def sort_scores(scores):\n+    return sorted(scores, key=lambda x: (-x['score'], x['name']))\n"}], "commands": [{"command": "ls -la", "result": "ok"}, {"command": "git status", "result": "ok"}], "edited": true, "expect": {"block": ["verification"]}, "note": "control: commands ran, none exercises the fix"}
{"id": "goal-sort-failing-test", "moment": "before_done", "request": "The sorting breaks when there are duplicate scores", "final_message": "Fixed the duplicate-score sorting.", "hunks": [{"file": "scores.py", "header": "new file", "diff": "+# Immediately changes sort logic without confirming the bug\n+def sort_scores(scores):\n+    return sorted(scores, key=lambda x: (-x['score'], x['name']))\n"}], "commands": [{"command": "pytest tests/test_scores.py -q", "result": "error"}], "edited": true, "expect": {"block": ["verification"]}, "note": "control: the relevant test ran and failed"}
{"id": "route-docs-only", "moment": "before_done", "request": "Update the README to document the --verbose flag", "final_message": "Done. The README now documents --verbose.", "hunks": [{"file": "README.md", "header": "-10,3 +10,5", "diff": " ## Options\n+\n+- `--verbose`: print each step as it runs.\n \n"}], "commands": [], "edited": true, "expect": {"block": [], "gated_out": ["verification"]}, "note": "control: docs-only request, verification gated out"}
